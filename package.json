{
  "name": "dotenvx-tui",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive CLI for managing encrypted env vars via dotenvx",
  "scripts": {
    "start": "bash env-manage.sh"
  }
}
